# Границы поля по протоколу rcssserver (move: x in [-54,54], y in [-32,32])
FIELD_WIDTH = 108   # 54*2
FIELD_HEIGHT = 64   # 32*2
# Запас от края — разворачиваемся, не доходя до линии
FIELD_MARGIN = 5
# Диапазоны для начальной позиции (не выходить за пределы)
MOVE_X_MIN, MOVE_X_MAX = -54, 54
MOVE_Y_MIN, MOVE_Y_MAX = -32, 32

# Численная точность для геометрии
EPS = 1e-9
